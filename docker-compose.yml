version: '3.7'

services:
  nix-sgx-sdk:
    image: nix-sgx-sdk
    build:
      context: .
    volumes:
      - ./asldobjdump.nix:/usr/src/asldobjdump.nix
      - ./sgxsdk.nix:/usr/src/sgxsdk.nix
      - ./shell.nix:/usr/src/shell.nix
      - ./nix:/usr/src/nix
